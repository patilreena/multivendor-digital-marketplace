<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <%= link_to "MARKETPLACE", root_path, class: "btn btn-outline-light logo text-white" %>
  <ul class="navbar-nav ml-auto">
    <li class="nav-item">
      <%= form_tag(products_path, :method => "get", id: "search-form") do %>
      <%= text_field_tag :search, params[:search], placeholder: "Search Products", class: "form-control" %>
      <% end %>
     </li> 
    <li class="nav-item">
      <% if user_signed_in? %>
      <%= link_to('Logout', destroy_user_session_path, method: :delete, :class => "nav-link mr-4 text-white") %>      <% else %>
      <%= link_to('SIGN IN', new_user_session_path, :class => "nav-link mr-4 text-white")  %>
      <% end %>
    </li>
    <li class="nav-item">
      <% unless user_signed_in? %>
      <%= link_to('JOIN NOW', new_user_registration_path, :class => "nav-link mr-4 text-white")  %>
      <% end %>
    </li>
    <li class="nav-item">
      <% if user_signed_in? && current_user.has_role?(:seller) %>
        <%= link_to('Dashboard', seller_dashboard_path, :class => "nav-link mr-4 text-white")  %>
      <% end %>
    </li>
    <li class="navbar-item">
      <%= render 'layouts/cart_text' %>
    </li>
  </ul>
</nav>

